@page
@model OttersDatabase.Pages.DeleteModel
@{
    ViewData["Title"] = "Delete";
}


<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Vydra</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Otter.Name</dd>
        <dt class="col-sm-2">Color</dt>
        <dd class="col-sm-10">@Model.Otter.Color</dd>
        <dt class="col-sm-2">Mother</dt>
        <dd class="col-sm-10">@(Model.Otter.Mother?.Name ?? "neznámá")</dd>
        <dt class="col-sm-2">Place</dt>
        <dd class="col-sm-10">@Model.Otter.Place.Name</dd>
        <dt class="col-sm-2">Location</dt>
        <dd class="col-sm-10">@Model.Otter.Location.Name</dd>
        <dt class="col-sm-2">Founder</dt>
        <dd class="col-sm-10">@Model.Otter.founder.UserName</dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Otter.TattooID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
